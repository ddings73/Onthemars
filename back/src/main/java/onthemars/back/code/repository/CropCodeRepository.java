package onthemars.back.code.repository;

import onthemars.back.code.domain.CropCode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CropCodeRepository extends JpaRepository<CropCode, String> {

}
